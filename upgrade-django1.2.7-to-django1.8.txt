We are upgrading the project from django1.2.7 to django1.8
this file will record what we are making to migrations

1.url retrives:
  {% url view_name args %} to {% url 'view_name' args %}
  use this:find . -type f -print0 | xargs -0 sed -i -r -e "s#\{% url ([a-zA-Z0-9_.:-]+)#\{% url '\1'#g"
2.urls.py:
  change from django.conf.urls.defaults *
  to     from django.conf.urls import patterns, include, url
3.localflavor
  change from django.localflavor... import ...
  to     from localflavor.... import ...
  TODO 
  * pip install localflavor
  * INSTALLED_APPS = (
    # ...
    'localflavor',
    )
4.crfs_token
  add {% crfs_token%} for forms.submit
5.messages
  change user.message_set.add()
  to     messages.add_message(request, messages.INFO, messages)
